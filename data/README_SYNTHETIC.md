# Data directory

Contains only non-identifying artifacts: extraction vocabularies
(med_synonyms.json, condition_synonyms.json, rxnorm_map.json), the clinical rules
(rules/clinical_rules_expanded.json), and a synthetic physician-authored scenario
library (scenario_library.csv). No real patient data are included. Evaluations in
the manuscript used authorized source data that is not distributed here.
